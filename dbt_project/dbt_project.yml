name: 'taxi_nyc_project'
version: '1.0.0'
config-version: 2

profile: 'taxi_nyc_project'

model-paths: ['models']
analysis-paths: ['analysis']
test-paths: ['tests']
seed-paths: ['seeds']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

target-path: 'target'
clean-targets: ['target', 'dbt_packages']

models:
  taxi_nyc_project:
    bronze:
      +schema: bronze
    silver:
      +schema: silver
    gold:
      +schema: gold
